name: Deploy to Dev Environment

on:
    push:
        branches:
            - develop
    workflow_dispatch:

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - run: echo 'Deploy to dev environment triggered by push to develop'
